---
const { date } = Astro.props;
---

<time class="text-sm sm:text-base" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-in", {
      month: "short",
      year: "numeric",
    })
  }
</time>
